# Available options:
# adb: Start Shizuku with adb privileges
# root: Start Shizuku with root privileges
mode=adb

# Available options:
# AUTO_SEARCH: Automatically search for libshizuku.so in /data/app/ and use the first result
# Manually specify the path to libshizuku.so, which is the recommended option for safety reasons
libshizuku=

# Config version, do not change
versionCode=1